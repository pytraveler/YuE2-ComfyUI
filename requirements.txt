tiktoken>=0.7
requests>=2.28

# YuE2 Write Song runs faster with llama-cpp-python, which is deliberately not
# listed above: on Windows it builds from source, and a pack that takes ten
# minutes to install -- or fails to -- for one optional node is not worth it.
# Nothing waits for it either. Without the wheel the node fetches about 32 MB of
# official llama.cpp binaries and writes the song in a subprocess, which was
# measured at five usable songs out of six against the wheel's six out of six.
# Prebuilt wheels, for anyone who wants the in-process path, are at
# https://github.com/abetlen/llama-cpp-python/releases
